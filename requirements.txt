fastapi==0.116.1
uvicorn==0.35.0
pydantic==2.11.7
pytest==8.4.1
httpx==0.28.1
# Wind map GIF generation
# On macOS, eccodes must be installed first: brew install eccodes
# On Debian/Ubuntu: apt-get install -y libeccodes-dev
meteofetch
matplotlib
Pillow
numpy